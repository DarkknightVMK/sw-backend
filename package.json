{
  "name": "sw-dummy-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Zero-dependency Node stand-in for the LittleWorlds/sw-backend API, for local testing without PHP.",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs"
  }
}
